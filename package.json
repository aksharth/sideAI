{
  "name": "webby-sider-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd siderAI && npm ci && npm run build",
    "start": "cd siderAI && npm start",
    "postinstall": "cd siderAI && npm ci"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

